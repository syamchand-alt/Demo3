Strings s="Welcome to java Peogram"
   int i,count=0;
  for(i=0;i<s.length();i++)
    {
  char ch=s.char At(i);
      if (ch == ' ' )
            count + +;
     }
       system.out.print(count +1)
       String .str[] = S.splict (" ") ;
       system.out.Print(str.length);
